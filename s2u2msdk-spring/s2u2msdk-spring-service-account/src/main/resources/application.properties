#server.port=9090

# evn setting
spring.profiles.active=local

# mybatis config
mybatis.config-location=classpath:config/mybatis-config.xml
mybatis.mapper-locations=classpath:mapper/**/*.xml
mybatis.type-aliases-package=s2u2msdk.spring.service.account.entity
mybatis.mappers-package=s2u2msdk.spring.service.account.dao
